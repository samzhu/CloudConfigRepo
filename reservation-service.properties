port=${random.int[1024,65536]}
server.port=${port}
#server.port=${random.value}
message=I am Sam!
#spring.cloud.stream.bindings.input=reservations
eureka.instance.metadataMap.instanceId=${spring.application.name}:${port}
